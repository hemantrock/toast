@using TOAST.Data.Model;
@model List<tblTagPageLink>
@{
    ViewBag.Title = "Index";
    Layout = null;
}
@*<div class="row">
    <div class="col-lg-12 text-right">
        <a id="createButton" role="button" class="btn btn-primary" href="#" onclick="OpenAddTagPageLink()">
            <span class="glyphicon glyphicon-plus"></span>CREATE NEW TAG PAGE LINK
        </a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <h3 class="grid-header">TAG PAGE LINK DETAILS</h3>
        <table id="tblTAG" class="table">
            <thead>
                <tr>
                    <th>TAG NAME</th>
                    <th>CONTROL NAME</th>
                    <th>PAGE NAME</th>
                    <th>CONTROL TYPE</th>
                    <th>CLASS NAME</th>
                    <th class="text-center">Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (tblTagPageLink itm in Model)
                {
                    <tr class="clickable" onclick="EditRecord('@itm.TagName', '@itm.PageName', '@itm.ControlName');">
                        <td>@itm.TagName</td>
                        <td>@itm.ControlName</td>
                        <td>@itm.PageName</td>
                        <td>@itm.ControlType</td>
                        <td>@itm.ClassName</td>
                        <td class="text-center">
                            <span id="spanDelete_@itm.TagName" class="glyphicon glyphicon-remove clickable spanDelete" 
                                  data-id="@itm.TagName" data-pageName="@itm.PageName" data-controlName="@itm.ControlName"></span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>*@

<div id="jqxgrid" class="jqx-tbl-container">
</div>	

<script src="~/Scripts/PageJS/Masters/TagPageLink/Index.js"></script>