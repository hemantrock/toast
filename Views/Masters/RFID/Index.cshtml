@using TOAST.Data.Model;
@model List<TOAST.Data.Model.tblRFIDMaster>
@{
    ViewBag.Title = "Index";
    Layout = null;
}
@*<div class="row">
        <div class="col-lg-12 text-right">
            <a id="createButton" role="button" class="btn btn-primary" href="#" onclick="OpenAddRFID()">
                <span class="glyphicon glyphicon-plus"></span>CREATE NEW RFID CARD
            </a>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <h3 class="grid-header" style="color:black">RFID Master</h3>
            <table id="tblRFID" class="table">
                <thead>
                    <tr>
                        <th>Tag No</th>
                        <th>EPC No</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Truck No</th>
                        <th class="text-center">Delete</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (tblRFIDMaster itm in Model)
                    {
                        <tr class="clickable" onclick="EditRecord('@itm.UniqueID');">
                            <td>@itm.TagID</td>
                            <td>@itm.EPC_Code</td>
                            <td>@itm.tblRFIDType.TypeDesc</td>
                            <td>@itm.tblRFIDStatusMaster.StatusDesc</td>
                            <td>@itm.tblTruckMasters.Where(x => x.RFID == itm.UniqueID).Select(x => x.TruckNumber).FirstOrDefault()</td>
                            <td class="text-center"><span id="spanDelete_@itm.UniqueID" class="glyphicon glyphicon-remove clickable spanDelete" data-id="@itm.UniqueID"></span></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>*@

<div id="jqxgrid" class="jqx-tbl-container">
</div>
<script src="~/Scripts/PageJS/Masters/RFID/Index.js"></script>
