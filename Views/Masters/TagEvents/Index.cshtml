@using TOAST.Data.Model;
@model List<tblTagEvent>
@{
    ViewBag.Title = "Index";
    Layout = null;
}
@*<div class="row">
    <div class="col-lg-12 text-right">
        <a id="createButton" role="button" class="btn btn-primary" href="#" onclick="OpenAddTagEvent()">
            <span class="glyphicon glyphicon-plus"></span>CREATE NEW TAG EVENT
        </a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <h3 class="grid-header">TAG EVENT DETAILS</h3>
        <table id="tblTagEvent" class="table">
            <thead>
                <tr>
                    <th>TAG NAME</th>
                    <th>LIMITER NAME</th>
                    <th>COMPARE VALUE</th>
                    <th>OPERATOR</th>
                    <th>EVENT</th>
                    <th>EVENT TAG TO NOTIFY</th>
                    <th>EVENT TAG VALUE</th>
                    <th>ACK TAG TO NOTIFY</th>
                    <th>FILTER TIME IN SECS</th>
                    <th>POPUP MSG</th>
                    <th class="text-center">Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (tblTagEvent itm in Model)
                {
                    <tr class="clickable" onclick="EditRecord('@itm.UniqueID');">
                        <td>@itm.TagName</td>
                        <td>@itm.LimiterName</td>
                        <td>@itm.Operator</td>
                        <td>@itm.CompareValue</td>
                        <td>@(itm.EventID!=null?itm.EventID:"")</td>
                        <td>@itm.EventTagToNotify</td>
                        <td>@itm.EventTagValue</td>
                        <td>@itm.AckTagToNotify</td>
                        <td>@itm.FilterTimeInSecs</td>
                        <td>@itm.PopUpMsg</td>
                        <td class="text-center">
                            <span id="spanDelete_@itm.UniqueID" class="glyphicon glyphicon-remove clickable spanDelete" data-id="@itm.UniqueID"></span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>*@
<div id="jqxgrid" class="jqx-tbl-container">
</div>

<script src="~/Scripts/PageJS/Masters/TagEvents/Index.js"></script>
