@using TOAST.Data.Model;
@model List<TOAST.Data.Model.tblTransporter>
@{
    ViewBag.Title = "Index";
    Layout = null;
}
@*<div class="row">
    <div class="col-lg-12 text-right">
        <a id="createButton" role="button" class="btn btn-primary" href="#" onclick="OpenAddTransporter()">
            <span class="glyphicon glyphicon-plus"></span>CREATE NEW TRANSPORTER DETAILS
        </a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <h3 class="grid-header" style="color:black">Transporters Master</h3>
        <table id="tblTransporter" class="table">
            <thead>
                <tr>
                    <th style="display: none;">Id</th>
                    <th>Transporter Code</th>
                    <th>Transporter Name</th>
                    <th class="text-center">Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (tblTransporter itm in Model)
                {
                    <tr class="clickable" onclick="EditRecord(@itm.UniqueID);">
                        <td style="display: none;">@itm.UniqueID</td>
                        <td>@itm.TransporterCode</td>
                        <td>@itm.TransporterDesc</td>
                        <td class="text-center"><span id="spanDelete_@itm.UniqueID" class="glyphicon glyphicon-remove clickable spanDelete" data-id="@itm.UniqueID"></span></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>*@

<div id="jqxgrid" class="jqx-tbl-container">
</div>	

<script src="~/Scripts/PageJS/Masters/Transporter/Index.js"></script>
