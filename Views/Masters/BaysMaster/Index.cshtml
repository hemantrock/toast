@using TOAST.Data.Model;
@using TOAST.Data.ViewModels;
@using System.Linq;

@model List<TOAST.Data.Model.tblBaysMaster>
@{ 
    var AllowEditPri = ViewBag.AllowEdit==true?1:0;
}
@*<div class="row">
    <div class="col-lg-12 text-right">
        <a id="createButton" role="button" class="btn btn-primary" href="#" onclick='OpenAddBaysMaster()'>
            <span class="glyphicon glyphicon-plus"></span>CREATE NEW Bays Master
        </a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <h3 class="grid-header">Bays Master</h3>
        <table id="tblBays" class="table">
            <thead>
                <tr>
                    <th>Gantry No</th>
                    <th>Bay Name</th>
                    <th>Bay ID</th>
                    <th class="text-center">Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var data in Model)
                {
                   
                    <tr class="clickable" onclick="EditRecord(@data.BayID);">
                        <td>@data.GantryNo</td>
                        <td>@data.BayName</td>
                        <td>@data.BayID</td>
                        <td class="text-center"><span id="spanDelete_@data.BayID" class="glyphicon glyphicon-remove clickable spanDelete" data-id="@data.BayID"></span></td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>*@
<input type="hidden" value='@AllowEditPri' id="AllowPriorityEdit" />
<div id="jqxgrid" class="jqx-tbl-container">
</div>	

<script src="~/Scripts/PageJS/Masters/BaysMaster/Index.js"></script>


