@using TOAST.Data.Model;
@model List<TOAST.Data.Model.tblCard>
@{
    ViewBag.Title = "Index";
    Layout = null;
}
@*<div class="row">
    <div class="col-lg-12 text-right">
        <a id="createButton" role="button" class="btn btn-primary" href="#" onclick='OpenAddCards()'>
            <span class="glyphicon glyphicon-plus"></span>CREATE NEW CARDS
        </a>


    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <h3 class="grid-header" style="color:black">Cards Master</h3>
        <table id="tblCards" class="table">
            <thead>
                <tr>
                    <th style="display:none;">ID</th>
                    <th>Card No </th>
                    <th>Status </th>
                    <th>Type</th>
                    <th>Card Expiry Date</th>
                    @*<th>MANAGER</th>
                    <th class="text-center">Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (tblCard itm in Model)
                {
                    <tr class="clickable" onclick="EditRecord(@itm.UniqueID);">
                        <td style="display:none;">@itm.UniqueID</td>
                        <td>@itm.CardNumber</td>
                        <td>@itm.tblCardStatusMaster.StatusDesc</td>
                        <td>@itm.tblCardPrivilegeMaster.PrivilegeDesc</td>
                        <td>
                        @if (itm.ExpiryDateTime != null)
                        {
                            @Convert.ToDateTime(itm.ExpiryDateTime).ToString("dd-MMM-yyyy")
                        }
                        </td>
                        <td>@if (itm.IsManager == true){
                            <text>Yes</text>
                            }
                            else
                            {
                            <text>No</text>
                        }
                        </td>
                        <td class="text-center"><span id="spanDelete_@itm.UniqueID" class="glyphicon glyphicon-remove clickable spanDelete" data-id="@itm.UniqueID"></span></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>*@

<div id="jqxgrid" class="jqx-tbl-container">
</div>

<script src="~/Scripts/PageJS/Masters/Cards/Index.js"></script>
